# 🌍 «گوشی را باز می‌کنم و کار می‌کند» — راه‌اندازی روی Render (رایگان)

هدف: یک آدرس اینترنتی دائمی که **روی گوشی باز کنی و کار کند** — بدون اینکه لپ‌تاپ روشن باشد
و بدون اینکه به چت/سندباکس دستیار وابسته باشد.

> چرا Render؟ رایگان است، **کارت اعتباری نمی‌خواهد**، و مهم‌تر از همه:
> وقتی بی‌کار بماند می‌خوابد ولی **خودش با اولین بازدید بیدار می‌شود** (~۵۰ ثانیه).
> یعنی «دائمی بودن» برایت مهم نباشد هم، همیشه در دسترس است.

---

## گام ۰ — کد روی گیت‌هاب (یک‌بار)

کد باید در مخزن گیت‌هاب تو باشد. سه راه (هرکدام راحت بود):

1. **از روی لپ‌تاپ:** `./github_publish.sh <token> <username> mega-ai`
2. **از داخل برنامه:** پنل → بخش 🔗 گیت‌هاب → توکن را بگذار → «پوش پروژه»
3. **توکن شخصی:** یک توکن کلاسیک با دسترسی `repo` بساز (راهنما: `GH_QUICK.md`)

> اگر مخزن را خصوصی (private) می‌سازی، Render هم با اجازهٔ خودت می‌تواند بخواند؛ اشکالی ندارد.

## گام ۱ — حساب Render

1. برو به <https://render.com> → **Get Started** → **GitHub** را انتخاب کن و اجازه بده.
2. اگر از ایران بدون فیلترشکن باز نشد، **VPN را روشن کن** (فقط برای همین ثبت‌نام).
3. کارت اعتباری لازم نیست (اگر خواست، همان لحظه پلن free را انتخاب کن).

## گام ۲ — ساخت سرویس (۲ کلیک)

فایل `render.yaml` از قبل در پروژه هست؛ پس:

1. Dashboard → **New** → **Blueprint**
2. مخزن `mega-ai` خودت را انتخاب کن → **Connect** → **Apply**

اگر Blueprint را پیدا نکرد، مسیر دستی همین است:
**New → Web Service** → مخزن → Language: **Docker** → Instance Type: **Free** → Health Check Path: `/api/health` → **Create Web Service**

## گام ۳ — صبر کن (۳ تا ۷ دقیقه)

اولین build طول می‌کشد (ffmpeg و پکیج‌ها نصب می‌شوند). وقتی وضعیت **Live** شد، آدرست این است:

```
https://<اسمی-که-دادی>.onrender.com
```

📱 همین آدرس را در گوشی باز کن — تمام. (حالت بدون کلید = مدل نمایشی؛ برای هوش واقعی گام ۴.)

## گام ۴ — کلید هوش (اختیاری ولی توصیه‌شده)

چون فایل `.env` روی سرور ابری **پاک می‌شود**، کلید را باید در خود Render بگذاری:

Dashboard → سرویس → **Environment** → **Add Environment Variable**

| Key | Value |
|---|---|
| `AVALAI_API_KEY` | کلید ایرانی (AvalAI) — بدون فیلترشکن |
| `GAPGPT_API_KEY` | یا این (GapGPT) |
| `OPENROUTER_API_KEY` | یا کلید بین‌المللی |
| `GITHUB_TOKEN` | اگر می‌خواهی برنامه خودش روی گیت‌هاب پوش کند |

بعد از ذخیره، سرویس خودش یک‌بار دوباره build می‌شود.

## گام ۵ — نکته‌های واقعی (بخوان تا شوکه نشوی)

| موضوع | واقعیت |
|---|---|
| خوابیدن | بعد از ۱۵ دقیقه بی‌کاری می‌خوابد؛ اولین بازدید ~۵۰ ثانیه طول می‌کشد (خودش بیدار می‌شود) |
| فایل‌های آپلودی / خروجی‌ها | روی پلن رایگان **موقتی**‌اند: با خواب‌رفتن یا redeploy پاک می‌شوند. خروجی مهم را دانلود کن یا بفرست به گیت‌هاب |
| حافظه | ۵۱۲ مگابایت — برای چت، کد، CSV، تصویر خوب است؛ ویدیوی سنگین کند می‌شود (برای آن لپ‌تاپ/VPS بهتر است) |
| ساعت رایگان | ۷۵۰ ساعت در ماه (بیشتر از یک ماه کامل) |
| کلید API | فقط در Environment؛ **هرگز** در مخزن گیت‌هاب |

## جایگزین‌ها

| گزینه | کی مناسب است |
|---|---|
| **لپ‌تاپ خودت + `python cloud_link.py`** | سریع‌ترین، بدون ثبت‌نام؛ لینک تا وقتی لپ‌تاپ روشن است زنده می‌ماند |
| **VPS ایرانی** | می‌خواهی همیشه بیدار باشد، بدون فیلترشکن، فایل‌ها بمانند: `docker compose up -d` |
| **Hugging Face Spaces** | ❌ دیگر برای اپ‌های Docker رایگان نیست (نیاز به پلن پولی) |
| **Koyeb** | ❌ پلن رایگانش حذف شده |
