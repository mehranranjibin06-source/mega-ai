# 🖥 ابرهوش روی سرور ویندوزی خودت (کنار ربات معامله‌گر)

**بهترین گزینه‌ی ممکن برای تو:** سرورت همیشه روشن است، پول اضافه نمی‌خواهد، کارت بانکی نمی‌خواهد،
و آدرسش تا وقتی سرور بالاست **همیشه ثابت می‌ماند**. فقط باید دو نکته را رعایت کنیم: رمز بگذاریم و
ربات را اذیت نکنیم.

---

## 📊 اول خیالت راحت: چقدر منابع می‌خورد؟

روی همین برنامه اندازه‌گیری شد:

| حالت | مصرف |
|---|---|
| بیکار (برنامه باز است، کاری نمی‌کند) | **~۶۵ مگابایت رم** · **۰٪ پردازنده** |
| موقع نصب اولیه (یک‌بار) | چند دقیقه پردازندهٔ بیشتر (وقت بازار نباشد بهتر است) |
| کارهای سنگین ویدیو | فقط وقتی خودت ویدیو بسازی (آن را در وقت آرام انجام بده) |

ربات معامله‌گر معمولاً چند صد مگابایت می‌خورد؛ این برنامه کنارش **چیزی حساب نمی‌شود**.

**🔒 نکته‌ی مهم درباره‌ی ربات:** ابرهوش در محیط مجازی خودش (`.venv`) نصب می‌شود؛ یعنی
**هیچ پکیج پایتونی ربات تو را دست نمی‌زند** و نسخه‌ها را عوض نمی‌کند. ربات و ابرهوش جداجدا زندگی می‌کنند.

---

## ✅ نصب در ۵ قدم

### قدم ۱ — بررسی سریع منابع (۱۰ ثانیه)
روی سرور `Ctrl + Shift + Esc` → تب **Performance**.
اگر **Memory Used** کمتر از ۷۰٪ و **CPU** بیکار زیر ۳۰٪ است، جای کافی داری. ✅

### قدم ۲ — پایتون ✅ (۳.۱۱.۹ تو تأیید شد)
پایتون **۳.۱۱.۹** روی سرورت نصب است و **کاملاً سازگار است** — همین‌جا تست شد:

| بررسی | نتیجه |
|---|---|
| اجرای سرور روی ۳.۱۱ | ✅ بالا آمد، `/api/health` = 200 |
| اجرای واقعی کد با موتور ایجنت | ✅ استریم زنده، بدون خطا |
| آپلود + تحلیل CSV | ✅ `ok: True` (نوع: data) |
| تست‌های سلامت (۹ تست) | ✅ همه سبز |
| نصب همهٔ کتابخانه‌ها | ✅ همه بستهٔ آمادهٔ ۳.۱۱ دارند |
| خطای ثبت‌شده در لاگ | ✅ صفر |

پس **هیچ کاری لازم نیست** — همین پایتون خوب است. (فقط باید ۳.۱۰ یا بالاتر باشد.)
اگر روزی پایتون نداشتی: **https://www.python.org/downloads/** → نصب کن و در صفحهٔ نصب
**تیک «Add python.exe to PATH»** را بزن.

### قدم ۳ — آوردن برنامه به سرور (بدون هیچ آپلودی)
در مرورگر سرور، همین آدرس را باز کن:
**https://github.com/mehranranjibin06-source/mega-ai**
→ دکمهٔ سبز **Code** → **Download ZIP** → فایل را در `C:\` اکسترکت کن تا بشود `C:\mega-ai`

### قدم ۴ — اجرا
داخل `C:\mega-ai` روی این فایل **دوبار کلیک** کن:

```
start_vps.bat
```

این فایل خودش:
1. پایتون را چک می‌کند
2. **یک رمز از تو می‌پرسد** ← یک رمز خوب بزن (فقط خودت بدانی). در `.env` ذخیره می‌شود.
3. **پورت ۸۰۰۰ را در فایروال ویندوز باز می‌کند**
4. برنامه را نصب و اجرا می‌کند (بار اول ۳ تا ۷ دقیقه)

> ⏰ **بهترین زمان اجرا:** وقتی بازار آرام است یا ربات در معامله نیست — فقط برای این‌که
> نصب اولیه چند دقیقه پردازنده را می‌گیرد.

### قدم ۵ — باز کردن روی گوشی 📱
آی‌پی سرورت را در گوشی بزن:

```
http://<آی‌پی-سرورت>:8000
```

(آی‌پی را در سایت‌هایی مثل whatismyipaddress از داخل سرور، یا در پنل هاست خودت می‌بینی)
بار اول مرورگر یک پنجرهٔ کوچک می‌آورد: **نام کاربری هرچه باشد مهم نیست** (مثلاً `1`) —
فقط **رمزی که در قدم ۴ گذاشتی** را بزن. تمام! از آن به بعد با گوشی کار می‌کنی. ✅

---

## 🔑 هوش واقعی (کلید API)

چون این سرور **مال خودت** است و همیشه بالاست، بهترین جا برای کلیدهای واقعی همین است:
- در پنل برنامه → بخش کلیدها → کلید ایرانی (AvalAI / GapGPT) را وارد کن
- یا در فایل `.env` کنار برنامه بگذار: `AVALAI_API_KEY=...`
- کلیدها فقط روی سرور خودت می‌مانند و هرگز جایی نمی‌روند.

---

## 🔁 اجرای خودکار بعد از ری‌استارت سرور (اختیاری)

می‌خواهی بعد از هر روشن‌شدن سرور، خودش شروع کند؟
1. `Win + R` → بنویس `shell:startup` → Enter
2. در پوشه‌ای که باز می‌شود، راست‌کلیک → **New → Shortcut**
3. مسیر: `C:\mega-ai\start_vps.bat`
4. تمام — دفعهٔ بعد خودش بالا می‌آید.

---

## 🧯 اگر مشکلی پیش آمد

| مشکل | راه‌حل |
|---|---|
| گوشی وصل نمی‌شود | پورت ۸۰۰۰ در فایروال باز است؟ (start_vps.bat بازش می‌کند) — از هاست خودت هم پورت را باز کن |
| رمز را یادم رفت | در فایل `.env` خط `MEGA_PASSWORD=` را عوض کن |
| ربات کند شده | کارهای ویدیویی را در وقت معامله انجام نده (فقط آن‌ها سنگین‌اند) |
| برنامه را می‌خواهم ببندم | همان پنجرهٔ سیاه را ببند |

---

## 🔁 جایگزین بدون بازکردن پورت

اگر نمی‌خواهی پورت را باز کنی: `start_cloud.bat` را بزن؛ یک آدرس HTTPS موقت می‌سازد
(بدون تغییر فایروال). ولی آدرسش با هر ری‌استارت عوض می‌شود.
