# ☁️ بردن MEGA-AI روی گیت‌هاب (و اجرا در فضای ابری)

سه کار می‌توانی بکنی — هر کدام مستقل است. **راه ۳** همان «باز کردن تو فضای ابری» است که خواستی (بدون نصب روی هیچ کامپیوتری).

---

## راه ۱) بدون کامپیوتر، فقط با گوشی: آپلود فایل ZIP

1. در مرورگر گوشی برو به **github.com** → وارد شو (یا ثبت‌نام کن — رایگان).
2. **+** بالا-راست → **New repository**.
   - Name: `mega-ai` · Public (یا Private) → **Create repository**.
3. در صفحه‌ی مخزن خالی: **uploading an existing file** را بزن.
4. فایل **`mega-ai.zip`** را از همین گفتگو دانلود کن و در گیت‌هاب آپلود کن → **Commit changes**.
5. تمام. حالا روی هر کامپیوتری می‌توانی از گیت‌هاب دانلودش کنی (`Code → Download ZIP`) و `start.bat` / `start.sh` را بزنی.

> نکته: ZIP آپلود‌شده «باز نشده» می‌ماند؛ روی لپ‌تاپ بعد از دانلود استخراجش کن.

---

## راه ۲) از لپ‌تاپ، یک‌دستوری: `github_publish`

اول یک مخزن خالی بساز (github.com/new، بدون README) و آدرسش را کپی کن، بعد:

**ویندوز:** روی **`github_publish.bat`** دوبار کلیک کن → آدرس مخزن را بچسبان → Enter.
اگر خطای احراز هویت داد، توکن بگذار:

- در گیت‌هاب: **Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token** با دسترسی `repo`.
- یک‌بار که توکن را گرفتی، در همان اسکریپت جای «توکن» بچسبانش.

**مک/لینوکس:**

```bash
./github_publish.sh https://github.com/USERNAME/mega-ai.git
# یا با توکن:
./github_publish.sh https://github.com/USERNAME/mega-ai.git ghp_xxxxxxxx
```

نتیجه: کل پروژه در مخزن تو می‌نشیند و از این به بعد با `git push` هر تغییری می‌رود بالا.
(فایل `mega-ai.bundle` هم در همین بسته هست: اگر روزی خواستی مخزن را بدون اینترنت منتقل کنی،
`git clone mega-ai.bundle mega-ai` کافی است.)

---

## راه ۳) ⭐ اجرا در فضای ابری — GitHub Codespaces (بدون نصب روی لپ‌تاپ)

وقتی پروژه در گیت‌هاب باشد، گیت‌هاب می‌تواند یک کامپیوتر ابری رایگان به تو بدهد و پنل را همان‌جا بالا بیاورد:

1. در صفحه‌ی مخزن، دکمه‌ی سبز **Code** → تب **Codespaces** → **Create codespace on main**.
2. منتظر بمان (۲ تا ۴ دقیقه بار اول). فایل `.devcontainer/devcontainer.json` که در پروژه گذاشته‌ام
   خودکار کارها را انجام می‌دهد:
   - نصب پایتون + ffmpeg + فونت‌ها + کتابخانه‌ها
   - بالا آوردن سرور `python3 server.py`
   - باز کردن پورت ۸۰۰۰ به‌صورت عمومی
3. یک پنجره / تب مرورگر باز می‌شود با پنل آسان. اگر باز نشد: پنل **Ports** را باز کن و روی آدرس پورت ۸۰۰۰ کلیک کن.
4. کلید API را در پنل (⚙️ تنظیمات) وارد کن. **دقت کن:** داده و کلیدت روی همان فضای ابری می‌ماند،
   پس اگر کار حساس داری، نسخه‌ی خانگی (روی لپ‌تاپ خودت) بهتر است.

**سهمیه‌ی رایگان:** برای حساب شخصی معمولاً ۶۰ ساعت در ماه (۲ هسته). برای تست و استفاده‌ی سبک کافی است.
اگر خواستی همیشه روشن بماند، Codespaces گزینه‌ی همیشه‌روشن نیست — برای آن به بخش «سرور ابری دائمی» پایین نگاه کن.

---

## به‌روزرسانی و نگه‌داری

| کار | دستور |
|---|---|
| گرفتن آخرین تغییرات روی لپ‌تاپ | `git pull` |
| فرستادن تغییری که خودت دادی | `git add -A && git commit -m "..." && git push` |
| نصب از صفر روی یک کامپیوتر تازه | `bootstrap.bat` (ویندوز) یا `./bootstrap.sh https://github.com/USER/mega-ai.git` |
| ساخت دوباره‌ی فایل قابل‌انتقال | `git bundle create mega-ai.bundle --all` |

---

## «سرور ابری دائمی» — اگر می‌خواهی ۲۴ ساعته در دسترس باشد

اگر هدفت فقط تست است، Codespaces کافی است. اگر می‌خواهی پنل همیشه بالا باشد (مثلاً از گوشی هرجا وصل شوی):

| سرویس | رایگان؟ | مناسب ایران؟ | نکته |
|---|---|---|---|
| **Hugging Face Spaces** | بله (CPU پایه) | معمولاً بله | Dockerfile ما را مستقیم قبول می‌کند؛ پورت را روی ۷۸۶۰ ست کن |
| **Render / Railway** | پلن رایگان محدود | نیاز به پروکسی/کارت | استقرار از همان مخزن گیت‌هاب، چند کلیک |
| **VPS ایرانی** | پولی (ارزان) | ✅ از داخل ایران | بهترین گزینه برای استفاده‌ی روزمره؛ فقط `docker compose up -d` |
| **کامپیوتر خودت + Cloudflare Tunnel** | رایگان | ✅ | `cloudflared tunnel --url http://localhost:8000` یک لینک عمومی می‌دهد |

برای Docker، همان `Dockerfile` و `docker-compose.yml` پروژه آماده است:

```bash
MEGA_PROXY= docker compose up --build -d     # روی سرور
docker compose logs -f                       # دیدن لاگ
```

---

## چیزهایی که **نباید** برود بالا (خودکار جلویش گرفته شده)

فایل `.gitignore` این‌ها را کنار می‌گذارد و هرگز کامیت نمی‌شوند:

- `.env` → کلیدهای API تو
- `data/` و `workspace/` → تاریخچه، فایل‌ها و خروجی‌های شخصی
- `.venv/` و کش‌ها

پس اگر پروژه را Public هم بکنی، **کلید و داده‌ات منتشر نمی‌شود**. با این حال یک بار بعد از اولین push
در گیت‌هاب چک کن که `.env` در فهرست فایل‌ها نیست.

---

## اگر گیت‌هاب برایت باز نشد

- با همان VPN فعال امتحان کن (اسکرین‌شاتت نشان می‌دهد VPN داری ✅).
- جایگزین‌های در دسترس‌تر: **GitLab**، **Codeberg**، **Gitea** (روی VPS خودت).
  همه‌ی اسکریپت‌های ما با هر ریموتی کار می‌کنند؛ فقط آدرس را عوض کن.
- اگر هیچ‌کدام نشد: بدون گیت هم می‌شود؛ فایل `mega-ai.zip` یا `mega-ai.bundle` را با تلگرام/فلش منتقل کن.
