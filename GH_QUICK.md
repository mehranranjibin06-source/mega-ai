# 🔗 وصل‌کردن برنامه به گیت‌هاب — ۳۰ ثانیه

بعد از این کار، **خودِ برنامه** می‌تواند مخزن بسازد، پروژه را بفرستد و issue باز کند.
فقط یک‌بار لازم است توکن بدهی.

## گام ۱ — توکن بساز (روی گوشی هم می‌شود)

1. برو به **github.com/settings/tokens** (اگر روی گوشی هستی: منوی ☰ → Settings → Developer settings →
   Personal access tokens → **Tokens (classic)**).
2. **Generate new token (classic)** → اسم بده: `mega-ai`
3. **Expiration**: ۹۰ روز یا No expiration (خواستی).
4. در فهرست **Scopes** فقط **`repo`** را تیک بزن (کافی است).
5. پایین صفحه **Generate token** → توکنی که با `ghp_` شروع می‌شود را **کپی کن**
   (بعد از بستن صفحه دیگر نشان داده نمی‌شود).

## گام ۲ — در پنل بچسبان

1. پنل را باز کن → بخش **🔗 گیت‌هاب** را باز کن.
2. توکن را در کادر بچسبان → **🔗 تست و ذخیره**.
3. اگر نوشت «✅ وصل به گیت‌هاب — کاربر …» تمام است.

## گام ۳ — استفاده

| دکمه | چه می‌کند |
|---|---|
| 📚 مخزن‌های من | فهرست مخزن‌هایت |
| 📤 خروجی‌های این نشست | فایل‌هایی که در همین گفتگو ساخته‌ای (ویدیو، پوستر، کد، گزارش) |
| 🚀 خودِ پروژه MEGA-AI | کل برنامه در **یک کامیت** روی مخزن تو |

یا به کارگزار بگو:
- «این پروژه را روی گیت‌هاب من بگذار»
- «مخزن به نام forush بساز و خروجی‌ها را بریز توش»
- «یک issue بساز: باگ در صفحه‌ی سفارش»

## چیزهایی که هرگز فرستاده نمی‌شوند

`.env` (کلیدها) · `data/` · `workspace/` · `.venv/` · `node_modules/` · فایل‌های حجیم‌تر از ۸ مگابایت
و هر چیزی که در `.gitignore` خودت نوشته باشی. این‌ها با تست خودکار بررسی می‌شوند.

## اگر خطا دیدی

| پیام | یعنی | کار |
|---|---|---|
| «توکن نامعتبر یا منقضی است» | توکن غلط/پاک‌شده | توکن تازه بساز |
| «دسترسی کافی نیست» | تیک `repo` را نزده‌ای | توکن تازه با دسترسی repo |
| «این نام قبلاً استفاده شده (422)» | مخزنی با همین نام داری | اسم دیگری بزن یا همان را انتخاب کن |
| «سقف درخواست پر شده» | محدودیت موقت گیت‌هاب | چند دقیقه بعد |
| «پیدا نشد (404)» | مخزن خصوصی است یا نام اشتباه | نام را با `username/repo` بنویس |
