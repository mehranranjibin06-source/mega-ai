# 🚀 MehranAiShabestar — از اینجا شروع کن

> **هیچ چیزی لازم نیست دستی نصب کنی.** برنامه خودش همه‌چیز را نصب می‌کند:
> کتابخانه‌های پایتون (۱۷ مورد + وابستگی‌ها)، فونت فارسی، و حتی ffmpeg (برای ویدیو).
> فقط پایتون ۳.۱۰ یا بالاتر روی سیستم باشد.

---

## ⚡ راه ۱ — روی سرور خودت (ویندوز) ← توصیه‌شده برای تو

**۱.** این پوشه را در `C:\` بگذار (مثلاً `C:\mega-ai`)

**۲.** روی این فایل **دوبار کلیک** کن:

```
start_vps.bat
```

**۳.** یک رمز بپرسد → رمزت را بنویس و Enter (حروف نمایش داده نمی‌شوند، طبیعی است)

**۴.** صبر کن (بار اول ۳ تا ۷ دقیقه، بقیهٔ دفعات چند ثانیه)

**۵.** آخرش یک آدرس نشان می‌دهد → در گوشی باز کن:

```
http://آی‌پی-سرورت:8000
```
نام کاربری: `1` (هرچه باشد) — رمز: همان که گذاشتی

---

## ⚡ راه ۲ — روی لپ‌تاپ (ویندوز / مک / لینوکس)

| سیستم | فایل |
|---|---|
| ویندوز | `start.bat` |
| مک / لینوکس | `start.sh` |
| می‌خواهی گوشی هم وصل شود (لینک موقت) | `start_cloud.bat` |

خودش مرورگر را باز می‌کند: `http://localhost:8000`

---

## 🧠 هوش واقعی (کلید API)

بدون کلید هم برنامه کار می‌کند (حالت نمایشی). برای هوش واقعی:

**پنل → ⚙️ تنظیمات → کلید را بچسبان → «تست اتصال»**

کلیدهای ایرانی پیشنهادی (بدون فیلترشکن کار می‌کنند):
AvalAI · GapGPT · MetisAI · WinkAPI — یا هر کلید بین‌المللی (OpenRouter، DeepSeek، OpenAI…)

> کلیدها فقط روی سیستم خودت ذخیره می‌شوند (فایل `.env`) و هیچ‌جا نمی‌روند.

---

## 🎁 داخل این بسته چیست؟

| فایل | کار |
|---|---|
| `start_vps.bat` / `start_vps.py` | اجرا روی سرور + رمز + بازکردن پورت |
| `start.bat` / `start.sh` | اجرای سادهٔ محلی |
| `start_cloud.bat` | اجرا + لینک عمومی برای گوشی (QR می‌سازد) |
| `server.py` | خود برنامه (۳۹+ سرویس، پنل فارسی) |
| `mega/` | موتور: عامل خودمختار، تحلیل، رسانه، گیت‌هاب، پیش‌نیازها |
| `mega/prereqs.py` | **نصب خودکار همه پیش‌نیازها** |
| `web/` | پنل ساده + پنل حرفه‌ای (فارسی، موبایل‌پسند) |
| `assets/` | لوگو، فاوآیکون، فونت فارسی (وزیرمتن) |
| `DEPLOY_VPS.md` | راهنمای کامل سرور (مصرف منابع، اجرای خودکار) |
| `DEPLOY_RENDER.md` · `render.yaml` · `Dockerfile` | اجرا روی سرور ابری (اختیاری) |
| `GITHUB.md` · `GH_QUICK.md` | اتصال به گیت‌هاب + پوش کردن پروژه |
| `tests/` | ۱۹ تست خودکار (سلامت + گیت‌هاب) |

---

## 💻 نمونه‌ی کارهایی که می‌کند

- **چت چند-مدلی** (۱۶ سرویس، انتخاب خودکار بهترین مدل)
- **کد می‌نویسد و اجرا می‌کند** (پایتون، جاوااسکریپت، شل) — نه فقط پیشنهاد
- **فایل آپلود کن → تحلیل واقعی**: CSV/اکسل (نمودار می‌سازد)، PDF، کد، تصویر
- **تصویر/ویدیو/تبلیغ بساز** + صداگذاری فارسی (`edge-tts`) + زیرنویس
- **گیت‌هاب**: پوش پروژه و خروجی‌ها (بدون فرستادن فایل‌های خصوصی)
- **تلگرام**: دستور از تلگرام هم می‌گیرد (`mega/telegram_bot.py`)
- **پنل فارسی موبایل‌پسند** + حالت حرفه‌ای

---

## 🛟 اگر جایی گیر کردی

| مشکل | راه‌حل |
|---|---|
| پیام «python not found» | پایتون را از python.org نصب کن و تیک **Add python.exe to PATH** را بزن |
| پورت ۸۰۰۰ مشغول بود | برنامه خودش پورت آزاد بعدی را می‌گیرد و در همان پنجره می‌نویسد |
| گوشی وصل نمی‌شود | پورت را در فایروال باز کن: `netsh advfirewall firewall add rule name="MehranAi" dir=in action=allow protocol=TCP localport=8000` |
| ویدیو کار نمی‌کند | ffmpeg لازم است — برنامه خودش با winget نصبش می‌کند؛ دستی: `winget install -e --id Gyan.FFmpeg` |
| رمز را فراموش کردی | فایل `.env` را باز کن و خط `MEGA_PASSWORD=` را عوض کن |

**همیشه می‌توانی به‌جای فایل‌های `.bat` این را در `cmd` بزنی:**

```
python start_vps.py
```
